Please implement the plan in `plans/phase-5-bounded-review-fix-loop-and-resume.md`. Follow `.cursor/rules/ai-dev-loop-governance.mdc`, `.cursor/rules/ai-dev-loop-orchestrator-contracts.mdc`, `.cursor/rules/ai-dev-loop-state-and-schema-contracts.mdc`, `.cursor/rules/ai-dev-loop-codex-review-contracts.mdc`, `.cursor/rules/ai-dev-loop-loop-and-resume-contracts.mdc`, and the repo-local skills listed in the plan. Keep the architecture guardrails intact, follow the testing criteria, add/update automated tests where required, avoid assumptions, and stop to resolve any OpenQuestions before making dependent changes.

Pay special attention to the Phase 5 boundary: implement the complete bounded stage-review-fix loop and make `resume <run-id>` real. Reuse the same Cursor chat ID for every Cursor turn, resume the exact same Codex session ID for every review, forward Codex-authored fix prompts exactly as stored, decide outcomes only from schema-validated Codex JSON, and leave final repository changes staged. Do not implement abort, integrations install/uninstall, commits, pushes, resets, cleans, stashes, unstaging, global hook installation, or real model activity in tests.

The riskiest parts are correction-iteration Git safety and resume idempotency. Correction iterations may start with staged changes only when those staged changes match the previous orchestrator-recorded staged patch; otherwise fail safely. `resume` must not rerun completed agent turns when durable artifacts prove they completed, and it must preserve partial artifacts before retrying interrupted turns. Use fake `agent` and `codex` executables in tests and `uv` for validation.
