Please implement the plan in archive/implementation-history/plans/phase-9-optional-codex-review-model-and-reasoning.md. Follow every Cursor rule listed in the plan, especially the Codex review, state/schema, orchestrator, loop/resume, and documentation contracts. Preserve the architecture guardrails, follow the testing criteria, add or update all required automated tests, avoid assumptions, and stop to resolve any OpenQuestions before dependent changes.

Pay special attention to omission-based inheritance, independent optional model/reasoning overrides, backward-compatible persisted run state, exact-session resume, safe subprocess argv construction, and keeping real model activity out of tests. Update code, schemas, rules, and user documentation. Run the complete validation suite; only after it passes, install the current package in WSL with `uv tool install --force .` and verify the installed executable without starting or resuming a real user run.
