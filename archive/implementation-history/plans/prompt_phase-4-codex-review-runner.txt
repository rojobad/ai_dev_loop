Please implement the plan in `plans/phase-4-codex-review-runner.md`. Follow `.cursor/rules/ai-dev-loop-governance.mdc`, `.cursor/rules/ai-dev-loop-orchestrator-contracts.mdc`, `.cursor/rules/ai-dev-loop-state-and-schema-contracts.mdc`, `.cursor/rules/ai-dev-loop-codex-review-contracts.mdc`, and the repo-local skills listed in the plan, keep the architecture guardrails intact, follow the testing criteria, add/update automated tests where required, avoid assumptions, and stop to resolve any OpenQuestions before making dependent changes.

Pay special attention to the Phase 4 boundary: implement structured `logs/events.jsonl`, Codex review execution through `codex exec ... resume <exact-session-id>`, schema-validated review results, review artifact persistence, status/docs/tests updates, and no-finding or waiting-for-fix outcomes, but do not implement Cursor correction turns, the complete loop, full resume/abort behavior, integrations install/uninstall, commits, pushes, resets, cleans, stashes, or real model activity in tests.

Use the exact configured target-repository review skill from run state, such as `$review-staged-cursor-execution`; do not confuse it with this repository's local `.agents/skills/review-staged-changes`, which is only for reviewing `ai_dev_loop` implementation changes. Use `uv` for validation. Expected commands are `uv run python -m ruff format --check .`, `uv run python -m ruff check .`, `uv run python -m mypy src`, `uv run python -m pytest -q -s`, `uv run python -m build`, and `uv run ai_dev_loop --help`. Use fake `agent` and `codex` executables in tests.
