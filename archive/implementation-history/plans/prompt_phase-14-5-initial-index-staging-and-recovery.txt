Please implement the plan in `archive/implementation-history/plans/phase-14-5-initial-index-staging-and-recovery.md`. Follow every Cursor rule listed in that plan, preserve the architecture guardrails, follow the testing criteria, add/update automated tests, and stop to resolve any OpenQuestions before making dependent changes.

Pay special attention to preserving the clean staged-index check before the first Cursor turn while allowing post-Cursor index mutation in both initial and correction turns; retaining `git add -A` normalization and Codex review of the complete staged snapshot; and adding a typed, fingerprint-verified, immutable-source recovery path for initial staging failures that resumes staging without rerunning Cursor. Do not mutate the real incident run, invoke real agents/updaters, or alter target-application code.
