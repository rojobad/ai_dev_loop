Please implement the plan in archive/implementation-history/plans/phase-20-6-5-authenticated-rollover-and-capacity-auto-resume.md. Follow every repository-local Cursor rule and the plan's testing criteria. Preserve the authenticated staged Phase 20.6 snapshot already seeded in this isolated worktree; do not touch the original worktree or source run.

The operator selected the distinct `scheduler rollover {prepare,start,status,abort}` command group. Keep source runs immutable, use only fresh agent identities for rollover, preserve exact frozen runtime/artifact bindings, fence every Git mutation and CAS, make capacity parsing fail closed except for the valid null reached marker, and add failure-injection tests for abort/hold/CAS/cleanup boundaries.
