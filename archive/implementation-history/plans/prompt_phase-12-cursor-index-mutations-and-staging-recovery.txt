Please implement the plan in archive/implementation-history/plans/phase-12-cursor-index-mutations-and-staging-recovery.md. Follow every Cursor rule listed in the plan, especially the loop/resume, state/schema, Codex review, orchestrator, abort, recovery, and documentation contracts. Preserve all architecture guardrails, follow the testing criteria, add/update automated tests, and stop to resolve any OpenQuestions before dependent work.

Pay special attention to keeping the previous patch strict before Cursor while allowing Cursor index mutations afterward, final `git add -A` normalization, repository-history protections, byte-for-byte inclusion of the exact Codex fix prompt inside a deterministic correction envelope, post-Cursor content fingerprints, staging-checkpoint successor recovery, and explicit legacy `--adopt-current-cursor-output` semantics. Implement code, schemas, rules, tests, docs, fake E2E, and WSL installation only after validation passes. Do not recover or mutate any real user run, invoke real agents/updaters/models, or alter target-application code.
