Please implement the plan in `plans/phase-7-global-codex-integrations.md`. Follow `.cursor/rules/ai-dev-loop-governance.mdc`, `.cursor/rules/ai-dev-loop-orchestrator-contracts.mdc`, `.cursor/rules/ai-dev-loop-state-and-schema-contracts.mdc`, `.cursor/rules/ai-dev-loop-codex-review-contracts.mdc`, `.cursor/rules/ai-dev-loop-loop-and-resume-contracts.mdc`, `.cursor/rules/ai-dev-loop-abort-contracts.mdc`, `.cursor/rules/ai-dev-loop-global-integrations-contracts.mdc`, and the repo-local skills listed in the plan. Keep all architecture guardrails intact, add/update automated tests where required, avoid assumptions, and stop to resolve any unexpected ambiguity before making dependent changes.

Phase 7 implements global Codex integrations. Replace the placeholder `integrations install` and `integrations uninstall` commands with real behavior: install the global handoff skill at `$HOME/.agents/skills/ai-dev-loop-handoff/SKILL.md`, install a standalone SessionStart hook script at `$HOME/.codex/hooks/ai_dev_loop_session_start.py`, safely merge the hook registration into `$HOME/.codex/hooks.json`, improve `integrations status`, and update `doctor` and README. The hook must expose the exact current Codex session ID through `hookSpecificOutput.additionalContext`, store only minimal session metadata under XDG state, never read transcript contents, and never expose auth material.

Use temporary `HOME` and XDG directories in automated tests. Cover first-time install, idempotent install, existing unrelated hooks, invalid hooks JSON fail-safe behavior, uninstall preserving unrelated hooks, hook input/output, status JSON, and doctor checks. Do not run real model activity in tests, do not bypass Codex hook trust, and do not modify bounded-loop, resume, or abort semantics.

The user has approved real installation in this phase. After implementation and full validation pass, run `uv run ai_dev_loop integrations install` against the real user HOME, then run `uv run ai_dev_loop integrations status` and `uv run ai_dev_loop doctor`. Report installed paths and tell the user to open `/hooks` in Codex and trust the `ai_dev_loop` hook. If existing real user hook files are invalid or ambiguous, stop safely and do not overwrite them.

Validate with `uv run python -m ruff format --check .`, `uv run python -m ruff check .`, `uv run python -m mypy src`, `uv run python -m pytest -q -s`, `uv run python -m build`, `uv run ai_dev_loop --help`, `uv run ai_dev_loop integrations status`, and `uv run ai_dev_loop doctor`.
