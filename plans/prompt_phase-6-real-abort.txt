Please implement the plan in `plans/phase-6-real-abort.md`. Follow `.cursor/rules/ai-dev-loop-governance.mdc`, `.cursor/rules/ai-dev-loop-orchestrator-contracts.mdc`, `.cursor/rules/ai-dev-loop-state-and-schema-contracts.mdc`, `.cursor/rules/ai-dev-loop-codex-review-contracts.mdc`, `.cursor/rules/ai-dev-loop-loop-and-resume-contracts.mdc`, `.cursor/rules/ai-dev-loop-abort-contracts.mdc`, and the repo-local skills listed in the plan. Keep the architecture guardrails intact, add/update automated tests where required, avoid assumptions, and stop to resolve any unexpected ambiguity before making dependent changes.

Phase 6 is intentionally scoped to real abort only. Implement `ai_dev_loop abort <run-id>` so it can request termination of an active Cursor or Codex child process group, persist an abort request, mark the run `aborted`, preserve repository contents and staged changes, and leave existing run artifacts auditable. Add durable active-process metadata for streaming Cursor/Codex subprocesses and make the shared workflow engine observe abort requests before and after workflow actions.

Do not implement global Codex integrations in this phase: no global handoff skill, no SessionStart hook, no hook trust handling, no `integrations install`, and no `integrations uninstall`. Also do not add commits, pushes, resets, cleans, stashes, unstaging, destructive cleanup, or real model activity in tests.

Pay special attention to process safety. Abort must use process-group signaling only when active-process metadata is clearly tied to the selected run; if metadata is stale or ambiguous, fail safely rather than killing unrelated processes. If a Cursor or Codex child is killed because of a user abort, the workflow should end as `aborted`, not as a generic failure or timeout. Use fake `agent` and `codex` executables in tests and `uv` for validation.
