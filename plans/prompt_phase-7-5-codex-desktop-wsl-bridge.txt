Please implement the plan in `plans/phase-7-5-codex-desktop-wsl-bridge.md`. Follow `.cursor/rules/ai-dev-loop-governance.mdc`, `.cursor/rules/ai-dev-loop-orchestrator-contracts.mdc`, `.cursor/rules/ai-dev-loop-state-and-schema-contracts.mdc`, `.cursor/rules/ai-dev-loop-codex-review-contracts.mdc`, `.cursor/rules/ai-dev-loop-loop-and-resume-contracts.mdc`, `.cursor/rules/ai-dev-loop-abort-contracts.mdc`, `.cursor/rules/ai-dev-loop-global-integrations-contracts.mdc`, and the repo-local skills listed in the plan. Keep all architecture guardrails intact, add/update automated tests where required, avoid assumptions, and stop to resolve any unexpected ambiguity before making dependent changes.

Phase 7.5 adds explicit support for Codex Desktop on Windows with agents and CLIs running in WSL. Preserve the existing Phase 7 behavior as the `wsl-cli` target, and add a new `codex-desktop-wsl` target. For the desktop target, install Codex-visible skill and hook registration into the Windows Codex home, generate a hook command that invokes WSL explicitly, and keep the hook session metadata under WSL XDG state. Do not share the entire Windows `.codex` home with WSL, do not set WSL `CODEX_HOME` to `/mnt/c/.../.codex`, and do not symlink, copy, migrate, open, or inspect Codex SQLite databases across Windows and WSL.

Migrate the safety behavior from the untracked helper `scripts/share-codex-desktop-sessions.sh` into typed Python commands under `ai_dev_loop integrations sessions install/status/list/remove`. Creating the desktop rollout bridge must be explicit via the sessions command or an explicit bridge flag; `integrations install --target codex-desktop-wsl` should not silently create the `/mnt/c` symlink by default. The bridge may expose only desktop rollout session files through a nested symlink such as `~/.codex/sessions/from-desktop`; it must reject unsafe whole-home or whole-sessions symlinks.

Update CLI, integration status JSON/text, doctor checks, README, and the global integration governance rule as needed. Keep hook trust manual and report it as unknown when not safely detectable. Keep `prepare --codex-session-id` as the manual fallback. Do not change bounded loop, resume, abort, Cursor chat identity, Codex session identity, or review-result decision semantics.

Use temporary HOME, XDG directories, fake Windows homes, and monkeypatched detection in automated tests. Do not depend on real `/mnt/c`, `cmd.exe`, `wslpath`, or `wsl.exe` in tests. Cover target-aware install/status/uninstall, desktop hook command generation, desktop session bridge install/status/list/remove, unsafe symlink rejection, Windows hooks JSON preservation, invalid hooks JSON fail-safe behavior, and doctor/status JSON.

Do not run real installation into the user's Windows home, do not run real model activity, and do not run a real `codex exec resume` unless the user explicitly approves after automated validation. Validate with `uv run python -m ruff format --check .`, `uv run python -m ruff check .`, `uv run python -m mypy src`, `uv run python -m pytest -q -s`, `uv run python -m build`, `uv run ai_dev_loop --help`, `uv run ai_dev_loop integrations status`, `uv run ai_dev_loop integrations status --target codex-desktop-wsl`, `uv run ai_dev_loop integrations sessions status`, and `uv run ai_dev_loop doctor`.
