Please implement the plan in `plans/phase-8-final-acceptance-and-mkdocs-scaffold.md`. Follow `.cursor/rules/ai-dev-loop-governance.mdc`, `.cursor/rules/ai-dev-loop-orchestrator-contracts.mdc`, `.cursor/rules/ai-dev-loop-state-and-schema-contracts.mdc`, `.cursor/rules/ai-dev-loop-codex-review-contracts.mdc`, `.cursor/rules/ai-dev-loop-loop-and-resume-contracts.mdc`, `.cursor/rules/ai-dev-loop-abort-contracts.mdc`, `.cursor/rules/ai-dev-loop-global-integrations-contracts.mdc`, `.cursor/rules/ai-dev-loop-docs-acceptance-contracts.mdc`, and the repo-local skills listed in the plan. Keep all architecture guardrails intact, add or update automated tests where required, and stop to resolve unexpected ambiguity before making dependent changes.

Phase 8 is final acceptance hardening, package validation, real workstation validation, and MkDocs scaffolding. Do not write the final documentation content; Phase 8.5 will do that with another model. Add MkDocs and Material for MkDocs as focused dev dependencies, create root `mkdocs.yml` with `theme.name: material`, create a minimal `docs/index.md` test page, move `scripts/codex-desktop-wsl-sessions.md` to `docs/reference/codex-desktop-wsl-sessions.md` as a placeholder for Phase 8.5, ensure generated `site/` output is ignored, and verify `uv run mkdocs build --strict`. Keep docs content intentionally small and clearly preparatory.

The user approved real workstation installation and real model calls for this phase. Use `Ubuntu-22.04` if WSL distro detection is ambiguous. The user reports the Codex Desktop `/hooks` trust step is already complete; only pause if the install changes the hook definition in a way that requires renewed trust. You may run real `codex-desktop-wsl` integration install/status checks and a small bounded real model-backed smoke test, but do it carefully and record exact commands/outcomes in `phase-8-findings.md`. If no exact desktop session ID is available, stop instead of guessing. Do not use `--last`, do not bypass hook trust, and do not run the real smoke test against this source repository.

The deleted shell helper `scripts/share-codex-desktop-sessions.sh` must stay deleted. Move the remaining untracked reference file `scripts/codex-desktop-wsl-sessions.md` into `docs/reference/codex-desktop-wsl-sessions.md` as a Phase 8.5 placeholder, keeping only useful safety facts/TODOs and removing outdated project-specific references.

Add or complete disposable fake-agent/fake-codex E2E acceptance coverage. Verify the full package and CLI with native WSL temp variables: `git diff --check`, `uv run python -m ruff format --check .`, `uv run python -m ruff check .`, `uv run python -m mypy src`, `TMPDIR=/tmp TMP=/tmp TEMP=/tmp uv run python -m pytest -q`, `uv run python -m build`, and `uv run mkdocs build --strict`. Also verify a non-editable install path and key CLI smoke commands from the plan.

Do not create commits, tags, pushes, resets, cleans, stashes, unstaging, or destructive cleanup. Do not implement a destructive cleanup command; cleanup remains a manual documentation policy for Phase 8.5. Preserve privacy defaults: do not print full prompts, fix prompts, staged patches, raw JSONL, auth payloads, or transcript contents in normal output. At the end, create `phase-8-findings.md` with validation results, real workstation install notes, real model smoke notes, residual risks, and a clear Phase 8.5 documentation handoff.
